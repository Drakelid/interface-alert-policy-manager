<div class="alert alert-info" style="margin: 15px;">
    <p>IAPM settings — ingestion token, dry-run mode, default policy, receivers, retention — are managed on the plugin's own settings page.</p>
    <a class="btn btn-primary" href="{{ route('iapm.settings.edit') }}">Open IAPM Settings</a>
    <a class="btn btn-default" href="{{ route('iapm.overview') }}">IAPM Overview</a>
</div>
